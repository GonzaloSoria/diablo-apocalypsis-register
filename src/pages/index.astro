---
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
import Story from '../components/Story.astro';
import Characters from '../components/Characters.astro';
import { data } from '../data/data';
import Form from '../components/Form.svelte';
import Called from '../components/Called.astro';
---

<Layout title="Diablo Apocalypsis | Registro">
	<Hero 
		title={data.hero.title}
		subtitle={data.hero.subtitle}
		copy={data.hero.copy}
		cta_link={data.hero.ctaLink}
		cta_copy={data.hero.ctaCopy}
	/>
	<Story 
		title={data.story.title}
		copy={data.story.copy}
		cta_copy={data.story.ctaCopy}
	/>
	<Characters 
		title={data.characters.title}
		copy={data.characters.copy}
		heroes={data.characters.heroes}
	/>
	<!-- <Called 
		title={data.called.title}
		copy={data.called.copy}
	/> -->
	<Form />
</Layout>

